# dependencies
node_modules/

# build output
dist/
build/
.turbo/

# test / coverage
coverage/
*.tsbuildinfo

# env / secrets — never commit credentials
.env
.env.local
.env.*
!.env.example
!**/.env.example

# logs & OS
*.log
.DS_Store
Thumbs.db

# playwright
playwright-report/
test-results/
apps/mcp-server/.preview/
*.png.tmp

# pi agent harness state
.pi/

# browser evidence screenshot artifact
packages/preview/*.png

# 本机排版输出（生成物，不提交）
text_one_out/
examples/showcase-out/
examples/handoff-out/

# 本机文章工作区：原文、排版成品、封面图。
# 都是本机产生的而不是库的一部分，不提交。
# 注意 articlesRoot() 默认取 process.cwd()，所以工作文件会直接落在仓根；
# 如果你的文章放在子目录（如 articles/），把那个目录也加到这里。
articles/
complex_article.md
text_one.md
收敛水*.md
收敛水-封面.jpg

# 移植脚本的上游缓存（scripts/port-wemd-themes.mjs 用）
.cache/
